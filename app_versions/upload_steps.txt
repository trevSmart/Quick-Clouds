Upload .vsix to OpenVSX
    In terminal:
        1. npx -> will open new prompt terminal
        2. ovsx publish livecheckqualityforsalesforce-XXX.vsix -p ${token_in_vault}
    If token fails (could be) create a new one.

Upload .vsix to VS Marketplace
    If token is outdated -> create a new one from -> https://dev.azure.com/pgonzalez0607/Test%20Integration
    In terminal:
        1. npm install
        2. vsce publish -p ${token_in_vault} --allow-missing-repository

VSCE Publisher -| https://marketplace.visualstudio.com/manage/publishers/qualityclouds